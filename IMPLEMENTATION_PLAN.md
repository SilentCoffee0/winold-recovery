# WinOld Recovery — Implementation Plan (v0.1)

Eight milestones, each shippable as a tagged pre-release. Estimates assume one developer or one AI coding agent working full time; they are effort, not calendar promises. Every milestone ends with green unit + integration tests and the invariant watchdog (SAFETY_MODEL §8) armed.

Global definition of done for any milestone:

- Builds with `dotnet build -warnaserror`; `dotnet publish` single-file x64 succeeds; the EXE starts elevated on a clean Windows 11 VM.
- All new code has tests; the integration suite runs against the generated fixture Windows.old with the source watchdog and reports zero source changes.
- No secret canary appears in logs, DB exports, or UI dumps.
- CHANGELOG entry written; docs updated if behaviour changed.

## Current implementation status

- M0 item 1: implementation complete on 13 Sep 2026. The solution and all specified projects target `net10.0-windows`; strict Release builds and tests pass; the WPF shell uses the system Fluent theme; the application manifest embeds `requireAdministrator`, PerMonitorV2 DPI awareness, and `longPathAware`; and the x64 self-contained ReadyToRun publish produces one 69,411,706-byte EXE. Startup refuses Windows 8.1 and Windows 10 builds before 1809 (17763), and 32-bit processes.
- M0 item 1 manual check still required: launch the published EXE on a clean Windows 11 VM and confirm the UAC/elevated startup path interactively.
- M0 handoff item 2: implementation complete on 13 Sep 2026. Startup enables `SeBackupPrivilege` and `SeRestorePrivilege`; backup-mode source reads use `CreateFileW`; `SourceGuard` canonicalizes paths through existing handles, resolves short names and reparse aliases, and blocks writes beneath registered source roots; `SafeFs` centralizes write operations; purge authorization is root-scoped. Eleven Core safety tests pass, including named I1 write/move tests and path-trick coverage.
- M0 native APIs required by the copy engine (`GetDiskFreeSpaceExW`, `SetFileTime`, `MoveFileExW`) shipped with `NativeFile` and are used by CopyEngine. `GetFileInformationByHandleEx` (attribute tag) and `FSCTL_GET_REPARSE_POINT` shipped with the walker.
- M0 handoff item 3: implementation complete on 13 Sep 2026. `SessionDb` schema v1 contains all specified state tables and query indexes, uses WAL with `synchronous=NORMAL`, records migrations, rejects newer schemas, exposes read-only query connections, and serializes mutations through a bounded single-reader writer channel. Seventeen Core tests pass.
- M0 handoff item 4: implementation complete on 13 Sep 2026. `FixtureGen` creates two profiles, live-target and loop junctions, file/directory symlinks, long/offline/invalid-name/large-`node_modules` hazards, privileged deny-ACL/orphan-SID/EFS hazards, and canary-bearing recipe shells; it refuses non-empty targets and writes a secret-free manifest consumed by its self-check. Portable generation and self-check pass in integration tests.
- M0 item 4 external check passed 15 Sep 2026: elevated `tools/run-elevated-m0.ps1` with 100,000 files printed `Elevated FixtureGen and self-check passed.` (transcript `%TEMP%\WinOldRecovery-elevated-m0-771b81a389904c61ab69a0c82a7c5809.log`, SHA `d3a17e1`). Deny-ACL, EFS, and orphan SID were `Created`. Do not tag `v0.1.0-m0` while other PENDING_MANUAL rows remain.
- M0 plan work item 5: implementation complete on 13 Sep 2026. Startup creates the dated session folder, exports/temp directories, WAL database, initial session row, and rolling redacted log under `%LOCALAPPDATA%\WinOldRecovery\sessions`. Canary, sensitive-path, log-rotation, source-containment, and folder-layout tests pass.
- M0 handoff item 5 (technical spikes): partially complete on 13 Sep 2026. The published single-file console harness successfully parsed a copied Windows 11 Default-profile hive with `Registry` and loaded/round-tripped SQLite. ReadyToRun/non-ReadyToRun console size and warm-run measurements are recorded in `docs/spikes/`. Interactive Explorer long-path `/select,` passed 15 Sep 2026. Elevated deny-ACL/orphan-SID enumeration and reads passed 15 Sep 2026 (`tools/run-elevated-m0.ps1`). The real-VM cleanup-handler check and elevated WPF startup measurement on a clean VM remain explicitly pending in `docs/spikes/PENDING_MANUAL.md`.
- M0 CI/release work: implementation complete on 13 Sep 2026. GitHub Actions `ci.yml` restores, builds with `-warnaserror`, tests, publishes the x64 single-file EXE, and uploads it as an unsigned artifact. `release.yml` attaches that EXE and a SHA-256 checksum to GitHub Releases on `v*` tags. The `v0.1.0-m0` tag is not created while elevated/manual M0 acceptance checks remain pending.
- M1 item 1: implementation complete on 13 Sep 2026. Source discovery lists `Windows.old*` variants on injected fixed-volume roots, skips reparse-point namesakes, offers other volumes that contain both `Users` and `Windows` except the live system volume, inspects browsed folders without blocking when `Users` is missing, estimates the 10-day cleanup date from folder creation time, and queries `SetupCleanupTask` through `IProcessRunner` with read-only `schtasks /Query` arguments (2 s timeout, overlapped with the volume walk). The GUI shows MainWindow before that query so first paint is not blocked. Interrupted-restore peek overlaps creating the new session folder. Discovery does not write under candidate sources. Embedded classification JSON is cached. Bound `Sources` updates after `DiscoverAsync` run on the WPF dispatcher. Do not tag.
- M1 item 2: implementation complete on 13 Sep 2026. `FileSystemWalker` enumerates with `FileSystemEnumerator` and `RecurseSubdirectories = false`, records reparse points as leaves (tag + target, never descended), never opens cloud placeholders, marks EFS/invalid destination names/long paths, continues after access-denied directories, computes post-order aggregates, inserts in 5,000-row batches as 48-row SQLite `VALUES` statements, rollups folder sizes in 32-row `CASE` updates, reports progress every 250 ms, and checkpoints completed top-level directories for cancel/resume. Classify badges insert in 96-row `INSERT OR IGNORE` statements. Hash-during-scan kv uses 64-row upserts. Writer PRAGMA uses a 64 MB cache and memory temp store. Tests against a junction tree and the portable FixtureGen fixture confirm the live-profile target is not scanned and source timestamps are unchanged.
- M1 item 3: implementation complete on 13 Sep 2026. `ProfileDetector` lists `Users\*` minus `Default`/`Default User`/`All Users` and reparse namesakes, classifies human vs `Public (shared)` vs service accounts, finds standard folders by well-known names and by offline `User Shell Folders` (including Known Folder GUIDs), reports redirects that sit outside the source, lists custom top-level folders, and records legacy junction names. Profiles persist through `SessionDb`.
- M1 item 4: implementation complete on 13 Sep 2026. `DecisionEngine` stores User and SuggestedDefault rows separately (schema v2), materializes `eff_decision` for a user-decision subtree or a single suggested node, reports mixed subtree summaries, and undoes the last 50 user/single suggested actions. Classify writes suggested defaults in one transaction. Inherited values are computed, never stored.
- M1 item 5: implementation complete on 13 Sep 2026; the interactive FlaUI smoke passed 15 Sep 2026 from an elevated testhost against the published EXE (still skipped in CI unless `RUN_FLAUI=1`). The WPF shell shows the six-step bar, source-integrity status strip, Scan source list, Decide Cards for profiles and standard folders, Files view-mode chrome (Tree/Largest/Recent/Search/Unknown/Problems), a virtualized list with badges and an extended-selection context menu (Inspect, Open Folder, Restore, Leave Behind, Undecided, Copy path), and an Inspect pane. Scan runs `ScanOrchestrator` (profiles then walker). Open Folder uses `IProcessRunner` with `explorer.exe /select,`. Step buttons and the scan/search/purge fields expose `AutomationProperties.Name` / `AutomationId` for Narrator and FlaUI.
- Scan source picker (UX 2.1) lists two-line cards (path, kind, created date, profile count, estimated deletion) and a 10-day Setup Cleanup callout. Scan options sit under the list. The status strip space budget uses KB/MB/GB. Restore jobs show ○/▶/✔ glyphs. A passing verify report lists the L2 hash set and that Purge is available. Do not tag.
- M2 item 4 jump-to-tree complete on 16 Sep 2026. Selecting the High-value items or Regeneratable Decide card lists matching badge rows in All files. Inspect lists per-rule counts and Why when classify recorded a breakdown. Opening last scan rebuilds those counts and bytes from scan badges. Do not tag.
- M7 first slice complete on 13 Sep 2026. Redacted unhandled-exception dialog; CONTRIBUTING; bug template that requires a support bundle; FAQ/limitations/recipes/signing docs; ARM64 publish profile and CI/release assets with SHA-256; FlaUI smoke skipped without `RUN_FLAUI=1`. The About dialog shows version 0.1.0, the git commit when stamped, the commit date, and the independent-project trademark note.
- M7 sixth slice complete on 14 Sep 2026. Explorer `/select,` uses the nearest ancestor under 259 characters. FlaUI smoke launches the published EXE with `runas` when opted in; scan→purge uses a browsed TEMP fixture (`tools/run-flaui-e2e.ps1`). `tools/run-elevated-m0.ps1` is the elevated FixtureGen entry point. Manual attempt log in `docs/spikes/PENDING_MANUAL.md`: this console is Medium IL. Backup-privilege 100k and the 200 MB VHDX *preflight* probe passed 15 Sep 2026; the published 1M `--scan` memory ceiling passed the same day (`%TEMP%\WinOldRecovery-1m-scan-namefilter.txt`, 103.7 MiB peak). Published mid-copy kill-and-resume passed (`%TEMP%\WinOldRecovery-kill-copy-e2b2930055f4487dab397a387aef55cc.log`, 450/2000 killed then resume 2000 files). Runtime `ERROR_DISK_FULL` pause+resume passed (`%TEMP%\WinOldRecovery-runtime-diskfull-8adf34fe4bd6482f8e045b659f267d62.log`). Interactive Explorer long-path `/select,` passed 15 Sep 2026 (`tools/run-explorer-long-path.ps1`). FlaUI scan→purge passed 16 Sep 2026 (`tools/run-flaui-e2e.ps1`). `cleanmgr`, SignPath, and clean-VM startup remain pending. Do not tag `v0.1.0` or `v0.1.0-m0`.
- M7 seventh slice complete on 14 Sep 2026. Decide Files view pages children past 2,000 with Show more, Recent 7/30/90-day selector grouped by folder, Reveal in tree from Search, source-integrity text switches to Deleting… / Windows.old removed on purge, and step navigation is blocked while a restore is running. Follow-up: decision shortcuts ignore text boxes, Restore keeps the expanded tree, overwrite ticks refresh the button, glob search is case-insensitive, Recent timestamps match scan inserts. A new Scan clears prior tree/plan state and relocks Purge (I10). Mixed subtree labels, extended-selection Restore/Leave Behind, and Restore except regeneratable / only newer files. Do not tag.
- M7 eighth slice complete on 14 Sep 2026. Startup reopens the newest session whose copy journal is Started/Paused/Failed (including remaining unstarted items), shows a Resume overlay, and persists restore source/destination in session kv at Preview. Decide Cards add one-line “none found” rows for catalog apps that were not detected. Manual M0/M7 checks in `docs/spikes/PENDING_MANUAL.md` are still not passed. Do not tag.
- M7 ninth slice complete on 14 Sep 2026. Per-folder destination overrides (longest prefix wins) feed PlanBuilder; mixed subtrees show an 8-cell restore/leave/undecided byte bar; disk-full pause copy names the volume, remaining space to free, and Resume/Cancel; resume preflight uses unfinished plan bytes only. Do not tag.
- M7 tenth slice complete on 14 Sep 2026. Preview summary lists recipes, cloud/EFS/access-denied counts, and remaining Undecided items; merge-into-profile option; Start restore blocks on running recipe apps; restore progress plus Cancel that keeps copies; Verify appends recipe results. Do not tag.
- M7 twelfth slice complete on 14 Sep 2026. Decide Undo (Ctrl+Z) uses the 50-step stack; recipe component Restore/Leave Behind persist in session kv and feed PlanCard; purge writes `session-record.txt` in the session folder before delete. `.gitignore` now ignores only recovery session *data* (`/sessions/`, `**/WinOldRecovery/sessions/`) so `WinOldRecovery.Core.Sessions` source and tests are tracked. Do not tag.
- M7 eleventh slice complete on 14 Sep 2026. CopyTree/verify skip Offline/EFS/reparse together so a completed restore can verify; Scan shows Setup Cleanup task presence and next-run from read-only `schtasks /Query` (never `/Change`). Do not tag.
- M7 thirteenth slice complete on 14 Sep 2026. After a completed purge the session is read-only (Scan/Decide/Restore/Purge blocked). Verify re-run writes a new immutable report; failed jobs settle for I10 only with a typed reason on that report id. Restore step lists per-plan-item job status from the journal. Do not tag.
- M7 fourteenth slice complete on 14 Sep 2026. Smart-card Open Folder uses the recipe `source` fact with `explorer.exe /select,`. Do not tag.
- M7 fifteenth slice complete on 14 Sep 2026. Restore Pause (UX 5.1) journals `Paused`/`User` between jobs and inside CopyTree; resume treats `Paused` like `Started` so already-copied files are not Keep-Both’d. Verify has Go to Purge. Do not tag.
- M7 sixteenth slice complete on 14 Sep 2026. Inspect computes an on-demand destination conflict preview (existing dest files vs source size/time, no reparse follow). Preview Re-check re-runs the dry-run plan. Do not tag.
- M7 seventeenth slice complete on 14 Sep 2026. Restore progress matches UX 5.1 (`38 %` plus `~N min left` from copied-byte rate). Verify report lists size/timestamp and SHA-256 file counts (UX 5.2) instead of a single pass/fail sentence. Do not tag.
- M7 eighteenth slice complete on 14 Sep 2026. Inspect (UX 3.3) shows owner SID with “old account, no longer exists” when the SID does not map, plus NTFS attributes and formatted size/file counts. Do not tag.
- M7 nineteenth slice complete on 14 Sep 2026. Restore (UX 5.1) reports skipped reparse/cloud/EFS entries as `Warnings (N)` with a Show/Hide detail toggle. Do not tag.
- M7 twentieth slice complete on 14 Sep 2026. Space toggles tree expand/collapse; Ctrl+F focuses the Search box. Inspect shows oldest/newest mtime and the matching classification Why (UX 3.3). Do not tag.
- M7 twenty-first slice complete on 14 Sep 2026. I10: `PurgeAuthorization.Evaluate` reads journal and verify settlement from `SessionDb`; UI booleans cannot mint those two gates. Do not tag.
- M7 twenty-second slice complete on 14 Sep 2026. Purge (UX 7) shows verified job count, Undecided warning with Review undecided, estimated free space after delete, and the session record path. Do not tag.
- M7 twenty-third slice complete on 14 Sep 2026. Purge How-to-delete radios (Windows cleanup vs direct), Delete Windows.old, live progress, and Cancel that leaves a partial tree and reports it. Do not tag.
- M7 twenty-fourth slice complete on 14 Sep 2026. Scan options (UX 2.1): compute folder sizes (default on) and hash files smaller than 64 MB, recorded in session kv. Do not tag.
- M7 twenty-fifth slice complete on 14 Sep 2026. Recipe cards expose Inspect / Open Folder / Restore / Leave Behind (UX 6). Do not tag.
- M7 twenty-sixth slice complete on 14 Sep 2026. Files tree greys reparse rows with ⊘, target tooltip, and dash columns; skip/problem copy explains each category (UX 3.2). Do not tag.
- M7 twenty-seventh slice complete on 14 Sep 2026. Decision chips (● Restore, ◌ Leave Behind / suggested, ○ Undecided); suggested tooltip until confirmed; inherited decisions dimmed (UX 3.1–3.2). Do not tag.
- M7 twenty-eighth slice complete on 14 Sep 2026. Files view radios Tree / Largest / Recent / Unknown / Problems (UX 3.2); Search remains the search box. Do not tag.
- M7 twenty-ninth slice complete on 14 Sep 2026. Decide Cards list exposes Inspect / Open Folder / Restore / Leave Behind on personal-folder and recipe cards (UX 3.1). Inspect/Open stay available after purge; Restore/Leave Behind follow the session lock. Do not tag.
- M7 thirtieth slice complete on 14 Sep 2026. Recent view groups files under folder-path headers (UX 3.2) without WPF GroupStyle, so the list stays virtualized. Do not tag.
- M7 thirty-first slice complete on 14 Sep 2026. Problems column binds the explanation sentence (UX 3.2). Do not tag.
- M7 thirty-second slice complete on 14 Sep 2026. Status strip binds source-integrity and space-budget levels to system colors (UX 1, M7 a11y); scan summary always includes known apps and high-value items (UX 2.2). Decide after scan lists Apps then Personal folders; Later keeps Undecided. Do not tag.
- M7 thirty-third slice complete on 14 Sep 2026. Scan progress reports profiles found plus live file/folder/size and skip counters (UX 2.2). Do not tag.
- M7 thirty-fourth slice complete on 14 Sep 2026. Main window and app styles bind SystemFonts message family/size, layout rounding, and PerMonitorV2 is asserted (UX 9 / M7 a11y). Do not tag.
- Preview offers UX_SPEC §4 sub-folder policy radios (`Recovered` vs merge into the live profile) and conflict-policy radios (keep both, skip existing, overwrite). The WPF shell uses rounded Fluent controls and card lists. Do not tag.
- M1 performance tests complete on 14 Sep 2026. `NodeBrowser` loads child counts and mixed-subtree flags only for directories, batches badge reads, and uses a `COLLATE NOCASE` parent index (schema v3). Schema v4 adds `ix_nodes_session_name` and `ix_nodes_session_rel` for recipe-detect lookups. CI expands a 100k-child node in under 300 ms and pages 1M synthetic children under the 1.5 GB working-set ceiling. The 1M on-disk empty-file walk is skipped on GitHub Actions unless `WOR_SCALE_1M=1` (create/delete of 1M files exceeds `windows-latest`). The published EXE `--scan` / `--report` 1M memory probe passed 15 Sep 2026 (`%TEMP%\WinOldRecovery-1m-scan-namefilter.txt`, 103.7 MiB peak). Do not tag.
- I15 kill-process restore complete on 14 Sep 2026. `FindRestoredPath` stops at the first missing keep-both candidate instead of probing 1,000 names per file; CopyEngine reuses one copy buffer and loads overwrite approvals once per plan item. Integration `I15_KillProcessDuringCopyTree` kills `tools/RestoreHarness` mid-CopyTree of 50,000 files, resumes, and asserts byte equality, no `.winold-partial` leftovers, and a clean source watchdog. The harness is started without redirected stdio so a full pipe cannot stall the copy. Headless published kill-and-resume passed 15 Sep 2026 (`tools/run-kill-published-copy.ps1`); the WPF Resume overlay after a mid-copy kill passed 16 Sep 2026 (`tools/run-flaui-e2e.ps1`). Do not tag.
- Named SAFETY_MODEL tests complete on 14 Sep 2026. I1–I16 each have a named passing automated test: I4 (no whole-AppData plan), I7 (`RestoreHarness` copy is watched with `netstat -ano` for remote TCP/UDP; published `--scan` is watched the same way when `WINOLD_RECOVERY_EXE` is set, or via elevated `tools/run-i7-published-scan.ps1`), I9 (recipe execute writes only planned destinations), I10 (purge token null when any gate fails), I11 (reparse leaves), I12 (offline OpenRead), I13 (containment both ways), I14 (destination ACLs are not copied from source). I1 greps mutating file APIs (`File.Delete`/`Move`/`Copy`/`WriteAllText`/`WriteAllBytes`, `Directory.CreateDirectory`/`Delete`, `ZipFile.CreateFromDirectory`, `SetAccessControl`, `RegLoadKey`) outside `SafeFs`/`Purge`; SSH private-key hardening, headless reports, smoke destinations, Firefox places copies, and support-bundle zips go through `SafeFs`. Window title includes source path and step (UX 1). Do not tag.
- WSL default-user after register complete: skip when `wsl.conf` already has `[user] default=`; otherwise `getent` plus `--set-default-user` with a `wsl.conf` append fallback. Registration suffixes `-recovered` when `wsl -l -v` already lists the name. `ownCopy` (`wsl --import --vhd`) is Leave Behind by default. Do not tag.
- Game-save publisher folders (`.minecraft`, Epic, Ubisoft, EA/Origin, Battle.net, Riot, LocalLow counterparts) are badged in addition to Saved Games / My Games / Steam / Unreal / GOG. Steam userdata under `Program Files (x86)\Steam` is restored into `Saved Games\Steam userdata`. Save folders named `saves`/`save`/`SaveGames` under `steamapps\common` are restored into `Saved Games\<title>`. After a walker checkpoint those Steam library folders come from the node index (no listing of every title). Do not tag.
- I7 source scan refuses `HttpClient`/`TcpClient`/`Socket(` plus `WebClient`, `HttpWebRequest`, `UdpClient`, `SocketsHttpHandler`, `WebRequest.Create`, `FtpWebRequest`, `HttpListener`, and `Dns.GetHost`. Do not tag.
- README includes PRODUCT_SPEC §10 search phrases (`selectively restore from Windows.old`). Help catalog title “What can and cannot be recovered” is covered by `LocalHelpTests`. Copy path uses `ITextClipboard` and strips `\\?\`. Do not tag.
- Recipe L3 persist complete on 14 Sep 2026. `RecipeHost.CollectLevel3Async` verifies executed writes (not a re-plan that would Keep-Both existing dests) and inserts level-3 rows on the L0–L2 report. A failed L3 check fails `LastVerifyReportAllOk`, so I10 `verify-store` blocks purge even when UI `VerifyAllOk` is true. Do not tag.
- Git discovery complete on 14 Sep 2026. After a walker checkpoint, `GitRecipe` finds `.git` from the node index (not only `Projects`); without a checkpoint it still walks the disk. It cards the outermost working tree, reads `.config\git\config` when `.gitconfig` is absent, and `RecipeHost` persists Git risk badges onto scan nodes. Do not tag.
- VHDX L2 and Git ignore files complete on 14 Sep 2026. Verify hashes `.vhdx`/`.vhd` past the 64 MB scan cap (R7). Git config restore copies `.config\git\ignore` and `.gitignore_global`. Do not tag.
- Git Analyze repositories complete on 14 Sep 2026. The Inspect pane runs `GitAnalyze` through `IProcessRunner` (resolved `git.exe`, exact flags, `HOME` on the live profile). Porcelain interpretation updates card `risk` and replaces the Git tree badge. Clean-and-pushed suggests Undecided; missing Git is local-only work. Do not tag.
- Syncthing Edit mapping complete on 14 Sep 2026. Preview lists remapped folder paths with exists/missing, persists overrides in session kv, and `RecipeHost.PlanCard` applies them when rewriting `config.xml`. Do not tag.
- L2 hash sample complete on 14 Sep 2026. Verify hashes files ≤ 64 MB, VHDX/VHD, sensitive nodes, a deterministic 2 % sample of remaining files (min 200), and every file when `scan.hashDuringScan` is set. Do not tag.
- Git L3 verify complete on 14 Sep 2026. `GitRecipe.VerifyAsync` compares restored `.git/HEAD` text and dest copied-file lengths, then when git.exe answers, `rev-parse HEAD` plus porcelain status through `IProcessRunner`. Missing Git skips the process compare. `RecipeHost.CollectLevel3Async` awaits recipe verify so the WPF verify step does not block on `GetResult()`. Do not tag.
- Git offline ref parse complete on 14 Sep 2026. `GitOffline` reads HEAD, loose and packed refs, remotes with credentials stripped, stash, reflog time, and index mtime. Uncommitted/unpushed remain unknown until Analyze; reftable backends skip the ref walk. Do not tag.
- Firefox Primary Password detect complete on 14 Sep 2026. `Key4PrimaryPassword` copies `key4.db` to session temp and tries the empty master-password check on metadata (PBES2 AES-256 and 3DES). Set / not set / unknown is stored on the card. Logins are never decrypted. Do not tag.
- Git Analyze copy complete on 14 Sep 2026. Analyze copies `.git` to `session/tmp/git-analyze/` via `SafeFs` and invokes git with `--git-dir` on the copy plus `--work-tree` on the source (no `-C` on Windows.old). Do not tag.
- Git L3 source copy complete on 15 Sep 2026. `CompareRestoredAsync` copies source `.git` via `SafeFs` and uses `--git-dir`/`--work-tree`; `-C` is only used on Recovered/. Do not tag.
- Git vendored listing complete on 15 Sep 2026. Repos under `node_modules` / `.cache` / similar regeneratable folders are cards marked `[Git: vendored]`, excluded from status analysis, default Leave Behind, and still listed when nested in another working tree. Probe uses directory listing only. Do not tag.
- Recipe tree badges complete on 15 Sep 2026. Detectors other than Git/SSH attach Kind/Detail badges onto scan nodes (Firefox/Chrome/Edge, KeePass, GPG, WSL/Docker, Anki, Outlook, Thunderbird, VS Code, Windows Terminal, Obsidian). Syncthing still badges the home and now also `[Syncthing folder: <label>]` for folder paths under the old profile. Do not tag.
- PowerShell profile classification complete on 15 Sep 2026. `Documents\PowerShell\*.ps1`, `Documents\WindowsPowerShell\*.ps1`, and those `Modules` folders receive a High-value PowerShell badge (Restore). Shell-dotfile names include `.gitignore_global` and `.ideavimrc`. PostgreSQL `PG_VERSION` and MySQL `ibdata1` receive a High-value Database badge (Undecided). Do not tag.
- R9 remaining badge rules complete on 15 Sep 2026. VM `.vbox`/`.vmx`; `*.db` at 1 MB; Joplin `database.sqlite` and Logseq `config.edn`; NuGet/Maven/Gradle/Cargo configs; `.yarnrc.yml`; `*.crt`/`*.cer` only with a sibling key; Unreal `Saved\SaveGames` Restore; GOG Applications Undecided; Notion AppData regeneratable (never auto Leave). Do not tag.
- R1/SAFETY_MODEL §5 cache and secret badges complete on 15 Sep 2026. Browser Cache folders under User Data / Firefox, GPU driver caches, and `.ollama\models` are Regeneratable (never Leave). Login Data, Cookies, key4/logins, NTUSER.DAT, Microsoft\\Protect, and Microsoft\\Credentials are Sensitive. Do not tag.
- Chromium Local State labels complete on 15 Sep 2026. Card title uses `profile.info_cache.<dir>.name` plus `user_name`/`gaia_name`; `Last Version` is a fact; `os_crypt` is not stored. Do not tag.
- Chromium extension locales and Syncthing activity complete on 15 Sep 2026. `__MSG_*__` names resolve from `_locales`. Syncthing facts include last activity, probably-active, and SyncTrayzor `syncthing.exe` FileVersion. Do not tag.
- OpenSSH Server and WSL size facts complete on 15 Sep 2026. `ProgramData\ssh` is an Undecided card copied to `Recovered\OpenSSH-Server`. WSL cards expose `fileSize` and `lastModified`. Do not tag.
- Anki custom bases complete on 15 Sep 2026. Start-menu `.lnk` `-b` paths and hive `ANKI_BASE` are scanned; cards record `schema`, `backups`, `newestBackup`, and `addons`. After a walker checkpoint, Start-menu `.lnk` paths come from the node index, and media / `.colpkg` backup / `addons21` counts come from the node index excluding `media.trash`. L3 also requires dest file lengths to match the source. Do not tag.
- SSH unencrypted-key facts complete on 15 Sep 2026. OpenSSH `none` cipher is read from the PEM header only; cards also store key types and `config`/`known_hosts` counts. Do not tag.
- Syncthing GUI TLS and custom GUI complete on 15 Sep 2026. `https-cert.pem`/`https-key.pem` default Leave Behind; `gui\` restores when present. Do not tag.
- WSL allocated-size facts complete on 15 Sep 2026. `GetFileInformationByHandleEx` FileStandardInfo supplies `allocatedSize` beside `fileSize`. Do not tag.
- WSL Lxss facts and password-export badges complete on 15 Sep 2026. Offline `Lxss` supplies DefaultUid/version; `wsl.exe --version` uses `IProcessRunner`. Password-manager export names are Sensitive/Undecided. Do not tag.
- SSH EFS and Firefox profile-groups complete on 15 Sep 2026. Encrypted private keys set `efsKeys`. A `StoreID` in `profiles.ini`/`installs.ini` skips ini registration. Do not tag.
- SSH ACL verify complete on 15 Sep 2026. L3 requires dest file lengths to match the source, then fails if Everyone/Users/Authenticated Users have Allow on private keys, `config`, or `authorized_keys`. SYSTEM and Administrators are tolerated. Do not tag.
- Outlook PST search and GPG GNUPGHOME complete on 15 Sep 2026. PST roots include Desktop, Downloads, and Outlook AppData. After a walker checkpoint, PST/OST paths come from the node index. `gpg --list-secret-keys` uses `GNUPGHOME` on the restored folder. Do not tag.
- Thunderbird profiles.ini discovery complete on 15 Sep 2026. Relocated profiles under the old user are transplanted; paths outside the old profile are ignored. L3 verify requires dest file lengths to match the source, dest `profiles.ini` to list the recovered profile, and `key4.db` to be paired with `logins.json` or `logins.db`. Editor extension scripts are `install-extensions-<cli>.cmd`. Do not tag.
- Git config facts complete on 15 Sep 2026. Cards expose userName, userEmail, sshCommand, and credentialHelper. Scrub leaves helper visible and redacts username/token/`insteadOf` userinfo. Do not tag.
- Syncthing identity verify complete on 15 Sep 2026. L3 compares folder/device counts, cert device ID, and `key.pem` hash, and requires dest copied-file lengths to match the source. Do not tag.
- Chromium transplant version gate complete on 15 Sep 2026. `WINOLD_RECOVERY_CHROMIUM_TRANSPLANT=1` still required; dest `Last Version` must be present and at least as new as the source. L3 verify requires dest copied-file lengths to match the source, then re-parses dest `bookmarks.html` to the source bookmark count. Do not tag.
- WSL default-user command list complete on 15 Sep 2026. Cards expose `cat /etc/wsl.conf`, `getent passwd`, `--set-default-user`, and `--terminate` via `IProcessRunner` request factories; Detect never launches them. Do not tag.
- Syncthing CLI device-id verify complete on 15 Sep 2026. `VerifyAsync` runs `syncthing --home <dest> --device-id` through `IProcessRunner` when stdout is non-empty. Do not tag.
- Chromium Local State transplant complete on 15 Sep 2026. Flagged restore copies Bookmarks into `Profile N`, backs up dest `Local State`, and registers `info_cache` as `<name> (recovered)`. Dest `os_crypt` is kept. Do not tag.
- Chromium last-used fact complete on 15 Sep 2026. Cards store `History` mtime as `lastUsed`. Do not tag.
- GPG dest-ring merge hint complete on 15 Sep 2026. When dest `gnupg` exists, the card records `gpg --import` and restores to `gnupg.from-windows-old`. L3 verify requires dest file lengths to match the source, then runs `gpg --list-secret-keys` with `GNUPGHOME` on the restored folder; a missing gpg.exe is skipped. Do not tag.
- GPG and Outlook help pages complete on 15 Sep 2026. Catalog lists `gpg.md` and `outlook.md`. Do not tag.
- Firefox tabs/extensions export complete on 14 Sep 2026. `MozLz4` decodes `sessionstore.jsonlz4` (and backups) via K4os.Compression.LZ4; tabs.html uses the selected session entry; extensions.html lists profile add-ons with AMO guid search links. Do not tag.
- Firefox `profiles.ini` / `installs.ini` discovery complete on 14 Sep 2026. Relative and absolute profile paths are resolved under the old user profile; empty profiles are shown and default to Leave Behind; `Profiles\*` remains a fallback. Do not tag.
- Firefox L3 verify complete on 14 Sep 2026. Recovered `profiles.ini` must list the new profile; dest copied-file lengths must match the source; dest `places.sqlite` is copied to session exports for `PRAGMA integrity_check` and bookmark-count compare; `key4.db` must be paired with `logins.json` or `logins.db`. Do not tag.
- Firefox transplant folders complete on 15 Sep 2026. Allow-list includes session/bookmark backups, `extensions\`, `storage\`, `search.json.mozlz4`, `addonStartup.json.lz4`, and sqlite `-wal`/`-shm` sidecars. Regenerated lock files are skipped. Do not tag.
- Safety audit 14 Sep 2026: keep-both verification, CopyTree resume, destination junction cleanup, restore completion, purge journal/verify-store gates, and cleanmgr StateFlags arming. Findings and residual risk in `BUG_AUDIT.md`. Do not tag.
- FlaUI overlay smoke passed 15 Sep 2026. From an elevated testhost the smoke drives the published EXE: attach with UIA3, dismiss the first-run and interrupted-restore overlays, open and close Help, find Preview plan, click the Scan step, find the Purge step. The same test now continues through scan→decide→preview→restore→verify→purge on a browsed TEMP `OldInstall` folder (`WINOLD_RECOVERY_SMOKE_SOURCE` / `WINOLD_RECOVERY_SMOKE_DEST`, PreferManualDelete). It refuses a volume-root `Windows.old*` smoke path and will not click Scan unless the status line shows `Smoke fixture ready`. It never selects `C:\Windows.old`. A Medium IL testhost skips instead of failing (UIPI `Win32Exception` 5). The window wait allows for cold single-file extraction (14.1 s measured here, 1.0 s warm) and is overridable with `FLAUI_WINDOW_TIMEOUT_SECONDS`. Startup no longer opens unrelated session databases as a writer to look for an interrupted restore; leftover `session.db` files larger than 32 MiB are skipped, interrupted sessions whose source folder is gone are skipped, and FindLatest is cancelled after 2 seconds so 1M-scan leftovers cannot block MainWindow. Open last scan hydrates SQLite off the UI thread; mixed subtree summaries use immediate children so a 1M-node last-scan cannot freeze the dispatcher. Explorer long-path `/select,` passed 15 Sep 2026 (`tools/run-explorer-long-path.ps1`, 345-character leaf, Explorer selected the 251-character ancestor). FlaUI scan→purge passed 16 Sep 2026 (`tools/run-flaui-e2e.ps1`, 23.6 s). `cleanmgr`, SignPath, and clean-VM startup remain pending. Do not tag `v0.1.0` or `v0.1.0-m0`.
- M3 items 2–5: first slice complete on 13 Sep 2026. `PreflightChecker` enforces the I16 space margin (plan bytes + 5% + 1 GB) and invalid names; `CopyEngine` copies through `.winold-partial` then `MoveFileExW`, journals Started/Completed/Failed/Paused, keeps both on conflict (I2), skips reparse points, and resumes by skipping Completed items; `RestoreRunner` refuses to start when preflight blocks; `Verifier` records immutable L0–L2 rows. Preview/Restore/Verify commands exist in the shell.
- M3 overwrite/resume/disk-full slice complete on 13 Sep 2026. Preview lists per-file conflicts; Overwrite is blocked until each listed file is confirmed and stored in session kv; copy overwrites only those paths. Interrupted copies with a leftover `.winold-partial` resume from journal `Started` and leave no partials. Runtime `ERROR_DISK_FULL` pauses the job and does not delete existing destination files. The 200 MB VHDX *preflight* probe and the 1400 MB runtime pause/resume probe both passed 15 Sep 2026 (`tools/run-disk-full-vhdx.ps1`, `tools/run-runtime-disk-full-vhdx.ps1`).
- M4 first slice complete on 13 Sep 2026. `IRecipe` / `RecipeHost` persist cards and components, execute CopyFile/CopyTree/WriteContent through the copy engine without replacing file-tree plan rows, and run after Restore. After a walker checkpoint exists, Git/KeePass/Obsidian/Outlook/WSL/Windows Terminal/Steam library saves detect from the node index (no second profile walk). SSH copies with `.from-windows-old` conflicts and destination ACL hardening; Chromium/Edge export Netscape bookmarks and refuse password recovery; Firefox transplants allow-listed files into `*-recovered` and appends `profiles.ini`; Git writes a scrubbed `.gitconfig` and restores repository working trees. SmartCard renders the six questions.
- M4 export/analyze slice complete on 13 Sep 2026. Chromium History is copied to session temp and exported as HTML/CSV at Plan; SNSS `Session_*`/`Tabs_*` yield `tabs.html` at Plan; extensions list includes store links at Plan. Detect only checks that those files exist so looking-for-apps does not parse Bookmarks, walk Extensions, or decode SNSS. Firefox `places.sqlite` yields bookmark HTML and history CSV at Plan; detect does not store sessionstore or extensions HTML. SSH Execute records `ssh -G localhost` through `IProcessRunner` and L3 verify fails when that parse check exits non-zero; `GitAnalyze` emits the documented git.exe flags with `GIT_OPTIONAL_LOCKS=0` and `HOME` on the new profile. Help pages: `docs/help/browser-passwords.md`, `ssh-keys.md`, `git-repositories.md`. Chromium autofill CSV is exported from `Web Data` (`autofill` / `autofill_profiles` only; never `credit_cards`). New-profile bookmark transplant remains behind `WINOLD_RECOVERY_CHROMIUM_TRANSPLANT=1`.
- M5 Syncthing first slice complete on 13 Sep 2026. `SyncthingRecipe` detects Local/Roaming/ProgramData/SYSTEM/SyncTrayzor homes, and after a walker checkpoint also custom `--home` folders from the node index (`cert.pem` parent that contains `config.xml` and `key.pem`). Decide **Add a Syncthing folder…** picks a folder under the scanned Windows.old that the walk missed (`--home`/`STHOMEDIR`). It derives a device ID from the certificate DER, scrubs GUI secrets in card facts, rewrites `config.xml` with every folder `paused="true"` and old-profile paths remapped, copies identity+config only, never plans `index-v2` / `index-v0.14.0.db`, and uses `Syncthing.recovered` when a destination home already exists.
- M5 Anki/WSL/GPG slice complete on 13 Sep 2026. Anki copies collection+WAL+media and omits `media.trash`/`collection.media.db2`. Detect reads note/card counts without copying the collection or running `PRAGMA integrity_check` (that stays in L3 verify). WSL copies `ext4.vhdx` to `wsl\recovered`, verifies dest size matches the source then the `vhdxfile` header, and when the register component is Restore records `wsl --shutdown` then `wsl --import-in-place` through `IProcessRunner` against the copied VHDX only (never Windows.old). After a walker checkpoint, WSL disks come from the `ext4.vhdx`/`docker_data.vhdx` name index. Detect reuses the NTUSER.DAT copy from profile detection. Default-user commands stay on the card. GPG restores the keyring except `random_seed`/sockets/locks/`crls.d`.
- M5 R9 detector cards complete on 13 Sep 2026. KeePass copies `*.kdbx`/`*.kdb` plus sibling key files without putting vault bytes in card facts; L3 verify requires those dest keys and dest vault lengths that match the source. Outlook restores PST and leaves OST; verify fails if an OST was written or if a restored PST length does not match the source. Thunderbird transplants an allow-list plus Mail/ImapMail, excluding `panacea.dat` and `global-messages-db.sqlite`. VS Code/VSCodium/Cursor restore User settings and write `install-extensions-<cli>.cmd`; L3 verify requires that script to list every source extension id. State folders stay Leave. Windows Terminal uses `settings.from-windows-old.json` on conflict and verify fails if existing `settings.json` was overwritten or if dest settings length does not match the source. Obsidian copies vaults that contain `.obsidian`; verify requires dest `.obsidian` and dest file lengths that match the source. Game-save cards restore Saved Games / My Games / Steam userdata / Unreal SaveGames / launcher and Steam-compatible AppData folders; L3 verify requires dest save-file lengths to match the source. Zotero/Calibre/Joplin/Logseq library cards restore the folder and verify the catalog file plus dest file lengths. After a walker checkpoint, those folders come from the node index (Joplin: a parent named `Joplin` that contains `database.sqlite`, not only `%APPDATA%\Joplin`).
- M6 first slice complete on 13 Sep 2026. Purge is unreachable until verify succeeds. `PurgeAuthorization` mints a `PurgeToken` only when every I10 gate passes (verify, files checked, undecided acknowledged, typed folder name, custom-root confirmation, no restore job, executable/session/destination not inside the source). `PurgeExecutor` records `cleanmgr.exe /sagerun:777` then falls back to bottom-up delete that never follows reparse points. Session `purge-manifest.txt` and a support-bundle zip are written outside the source. `session-record.txt` is written in the session folder immediately before purge. Real-VM cleanup-handler deletion remains a pending manual check. 10-day warning shows Setup Cleanup task presence and next-run time from `schtasks /Query` LIST output.

---

## M0 — Foundation and technical spikes (≈ 1 week)

**Goal.** A compilable skeleton with the safety primitives, persistence, fixture generator, and answers to the open technical questions in ARCHITECTURE §12.

**Work.**
1. Create the solution per ARCHITECTURE §3; `Directory.Build.props` (net10.0-windows, nullable, warnings as errors, `LangVersion` latest); `app.manifest` (requireAdministrator, longPathAware, PerMonitorV2 DPI); publish profile (single-file, self-contained, compression, native extraction).
2. `WinOldRecovery.Native`: `CreateFileW` (backup semantics, open-reparse-point, no-recall), `GetFileInformationByHandleEx` (attribute tag), `FSCTL_GET_REPARSE_POINT`, `AdjustTokenPrivileges` helper (`Privileges.EnableBackupAndRestore()`), `GetDiskFreeSpaceExW`, `SetFileTime`, `MoveFileExW`.
3. `Core.SafeFs` + `SourceGuard`: all file APIs used by the app; source-root registry; refusal of any write under a source root without a `PurgeToken`; `\\?\` normalisation; unit tests including path-trick cases (`..`, short names, trailing spaces, different casing, junction-resolved paths).
4. `Core.SessionDb`: SQLite schema v1 (sessions, profiles, nodes, badges, decisions, cards, components, plan_items, journal, verify_results, kv), WAL mode, single-writer channel, migration scaffold.
5. Logging with redaction sink; session folder layout.
6. `tools/FixtureGen`: builds a synthetic Windows.old in a target directory (and optionally on a fresh VHDX): two profiles, legacy junctions pointing at a "live" fixture profile, junction loop, file + dir symlinks, deny-ACL folder, orphan-SID-owned files (create temp local user, own files, delete user), EFS files (`cipher /E`), 300-char paths, `OFFLINE`-attributed placeholder stand-ins, invalid-name files, 100k-file `node_modules` tree, and recipe fixtures with canary secrets (empty shells for now; recipes fill them in later milestones).
7. Spikes with written results in `docs/spikes/`: (a) `FileSystemEnumerator` lists the deny-ACL and orphan-SID folders with backup privilege enabled; (b) `CreateFileW`-backed `FileStream` reads them; (c) `Registry` NuGet parses the fixture `NTUSER.DAT` inside the single-file bundle; (d) SQLite native loads inside the bundle; (e) `cleanmgr /sagerun` with only the Previous Installations flag deletes a fake `Windows.old` on a test VM (or record that it needs the real handler and the fallback must be primary); (f) startup time and EXE size with/without ReadyToRun.
8. CI: GitHub Actions build + test + publish artifact on every push; release workflow on tags.

**Tests.** SafeFs/SourceGuard unit tests; SessionDb round-trip; FixtureGen self-check (asserts every hazard is present); spike tests as skippable integration tests.

**Acceptance.** Skeleton EXE runs elevated, shows an empty main window, writes a session folder; all spikes answered; fixture generator produces a tree that a naive recursive `Directory.GetFiles` would loop or fail on.

## M1 — Scan engine and Files tree (≈ 2 weeks)

**Goal.** Scan a real Windows.old into SQLite and browse it in a responsive tree with decisions.

**Work.**
1. Source discovery (all volumes, `Windows.old*`, roots with `Users`+`Windows`, browse), validation, age + estimated auto-delete date from folder creation time and `SetupCleanupTask` presence.
2. `FileSystemWalker` per ARCHITECTURE §5.1: manual recursion, reparse leaf nodes with tag and target, placeholder/EFS/access-denied problems, post-order aggregates, batched inserts, progress, cancel/resume (walker checkpoints per top-level directory).
3. Profile detection (human vs. service vs. Default/Public), standard-folder detection by name and by offline `User Shell Folders`.
4. Decision engine: explicit decisions, inheritance, effective-decision materialisation for subtrees, mixed-state summaries, undo (last 50 actions).
5. WPF shell: step bar, status strip (source integrity, space budget placeholder), Scan view, Decide view with `TreeListView` (virtualized, paged children, sorting, badges column, decision column), Detail pane, views Largest/Recent/Search/Unknown/Problems, Open Folder / Open Containing Folder, keyboard shortcuts, multi-select context menu.
6. Optional hashing stage (files < 64 MB) as a background job.

**Tests.** Walker against the fixture: node counts, aggregates, every hazard classified correctly, junction never followed (assert no node under the live-profile fixture path), watchdog clean. Decision inheritance property tests. Performance test: 1M synthetic entries scanned in < 60 s on CI hardware and tree expansion of a 100k-child node in < 300 ms. FlaUI smoke: scan fixture, expand, mark Restore/Leave, restart app, decisions persisted.

**Acceptance.** The reference 782k-file profile scans in under 60 s; the UI never blocks longer than 100 ms during scan or expansion; all Problems categories appear with explanations; nothing is written under the source.

## M2 — Classification, high-value and regeneratable rules, suggested defaults (≈ 1 week)

**Goal.** Badges and suggested defaults per PRODUCT_SPEC §7 and RECOVERY_RECIPES R1/R9.

**Work.**
1. Rules engine over embedded JSON rule files (`HighValue.rules.json`, `Regeneratable.rules.json`, `GameSaves.rules.json`, `Sensitive.rules.json`): path globs, sibling-file conditions, extension lists, size thresholds, header-signature checks (first 4 KB only, never for sensitive-by-path files).
2. Sensitivity flags and redaction wiring.
3. Suggested-default table and its application (marked `SuggestedDefault`, distinct in UI).
4. "High-value items" summary card and "Regeneratable" summary card (counts, bytes, per-rule breakdown, jump-to-tree).
5. Contributor documentation for rule files.

**Tests.** Rule unit tests per rule with positive/negative fixtures; test asserting no default `LeaveBehind` exists for unclassified or regeneratable nodes; canary test for sensitivity redaction.

**Acceptance.** On the fixture and on the reference machine, KeePass/GPG/VS Code/Obsidian/game-save/`.env`/VM-disk items are badged; `node_modules` and caches are badged, never decided; nothing unknown is called junk.

## M3 — Plan, Preview, Copy engine, Journal, Verify for personal folders (≈ 2 weeks)

**Goal.** End-to-end restore of file-tree decisions with all safety properties.

**Work.**
1. `PlanBuilder` (CopyTree/CopyFile items from effective decisions), destination mapping per profile (current user default, custom, "Recovered" subfolder policy), containment validation.
2. `PreflightChecker`: conflicts (per-file compare of size/time; listing), free space per volume with margin, invalid destination names, long-path warnings, running-app checks (interface used by recipes later).
3. `CopyEngine` per ARCHITECTURE §6: backup-semantics reads, partial-file + rename, conflict policies, timestamps, parallel workers, in-line SHA-256, disk-full pause, retries, journal.
4. `RestoreRunner`: jobs, progress, pause/cancel, resume-from-journal on next launch, warnings list.
5. `Verifier` L0–L2 with sampling policy and immutable reports.
6. Preview, Restore, Verify views per UX_SPEC §4–5, including the overwrite-approval file list and the space budget in the status strip.

**Tests.** Integration: restore fixture folders to a temp destination; assert byte-equality, timestamps, fresh ACLs (no orphan SIDs), keep-both naming, skip policy, overwrite only with approval. Kill tests: terminate the process at random points during a 50k-file restore; relaunch; resume; final state identical to an uninterrupted run; no `.winold-partial` leftovers; source watchdog clean. Disk-full test on a 200 MB VHDX destination: pauses, nothing deleted, resumes after freeing space. Verify report: injected corruption in one destination file is detected at L2.

**Acceptance.** All SAFETY_MODEL invariants I1–I3, I9, I11–I16 have named passing tests; a 40 GB restore on the reference machine runs at disk speed with live progress; cancel and resume work.

## M4 — Recipes batch 1: Firefox, Chromium, SSH, Git (≈ 2 weeks)

**Goal.** First smart cards with real recipes and the `IRecipe` pipeline.

**Work.**
1. `IRecipe` infrastructure: registry, `ProfileContext`, card/component persistence, `SmartCard` control with the six-question template, prerequisites (running processes), recipe steps in plans, L3 verification hooks, exports folder.
2. Firefox recipe R3: profile discovery (`profiles.ini`, `installs.ini`), facts, transplant with allow-list and `profiles.ini` registration, exports (bookmarks HTML, history CSV, tabs from mozLz4, extension list), Primary Password detection (attempt to open `key4.db` metadata; if it cannot be determined, say so).
3. Chromium recipe R2 for Chrome and Edge: discovery, facts, bookmarks JSON→Netscape HTML, history export, SNSS tab list, extension list with store links, autofill export, optional new-profile bookmark transplant behind a feature flag until the spike passes, fixed "cannot recover" component with copy.
4. SSH recipe R5: discovery, fingerprints, unencrypted-key detection, copy with no-overwrite naming, ACL hardening, `ssh -G` verify when available.
5. Git recipe R6: config restore with scrubbed display; repo discovery during scan (`.git` dir/file), offline ref parse, on-demand git.exe analysis with the exact flags, risk badges, restore as tree, L3 verify.
6. Help pages: "Browser passwords after reinstall", "SSH keys", "Git repositories".

**Tests.** Recipe fixtures with canary secrets: Firefox profile (real schema `places.sqlite`, `key4.db`, `logins.json`), Chromium profile (`Bookmarks`, `History`, SNSS v3 session, `Local State`), `.ssh` with encrypted and unencrypted keys, git repos in eight states. Assertions: exports correct (bookmark counts, tab URLs), transplant registers profile, secrets never in logs, ACLs correct, git analysis writes nothing (watchdog on `.git`), risk levels correct, offline mode conservative.

**Acceptance.** On the reference machine: Firefox profiles restore into a new Firefox as new profiles with passwords intact (manual check), Chrome bookmarks import from the generated HTML, the Chrome card states the password limitation, git repos show correct risk with and without git.exe.

## M5 — Recipes batch 2: Syncthing, Anki, WSL/Docker, GPG and high-value cards (≈ 2 weeks)

**Work.**
1. Syncthing recipe R4: multi-home discovery (including `ProgramData` and other profiles), device-ID derivation, config parse with secret scrubbing, config rewrite (paused, path remap, defaults), destination rules, prerequisites (processes + service), L3 verify.
2. Anki recipe R8: discovery incl. `-b` shortcuts, facts via read-only copy, profile restore with WAL, backups, add-ons, prefs rule, verify.
3. WSL recipe R7: offline `Lxss` parse, filesystem discovery, Docker Desktop disks, VHDX copy with mandatory hash, registration and default-user steps behind explicit confirmation, WSL 1 detection as manual, verify.
4. GPG, KeePass, Thunderbird, Outlook, VS Code, Windows Terminal, Obsidian: detector cards with copy-based restore and the specific rules in R9 (no-overwrite naming, `install-extensions.cmd` export).
5. Help pages: "Syncthing identity", "WSL", "Anki".

**Tests.** Fixtures: Syncthing home with generated cert (device ID precomputed) and config with three folders; Anki profile from an empty schema plus media; VHDX stub with valid header; gnupg tree; KeePass file; VS Code settings + extensions list. Assertions: rewritten config has every folder paused and remapped paths; device ID equal; index not copied; Anki collection integrity ok; VHDX hash equal; `wsl.exe` invocations are recorded but not executed in tests (process runner abstraction); canaries absent.

**Acceptance.** On the reference machine: Syncthing card shows two identities with correct last-activity and device IDs; restoring the active one to a fresh Syncthing install yields the same device ID with all folders paused; Anki opens the restored profile; Docker VHDX copies and verifies.

## M6 — Purge, session records, support bundle (≈ 1 week)

**Work.**
1. Purge view with all gates (UX_SPEC §7), typed confirmation, `PurgeToken` minting.
2. `PurgeExecutor`: OS cleanup handler path with polling and timeout; manual fallback (ownership, reparse-aware bottom-up delete, progress, cancel, residue report); refusal conditions (running jobs, executable/session/destination inside root, non-source path).
3. Session record export before purge; "Create support bundle".
4. 10-day warning polish: show task presence and next run; no modification.

**Tests.** Gate tests (each gate individually blocks); token cannot be minted outside the purge view-model; manual deleter on a fixture with junction loop, deny ACLs, long paths, read-only files completes and never touches junction targets (watchdog on the live-profile fixture); cleanup-handler path tested on a VM with a real `Windows.old` produced by an in-place reinstall (manual test checklist, recorded).

**Acceptance.** Purge cannot be reached without a verify report; purge of a fixture leaves nothing behind and the live fixture untouched; session records exist afterwards.

## M7 — Hardening, polish, docs, release (≈ 1–2 weeks)

**Work.**
1. FlaUI end-to-end: scan → decide → preview → restore → verify → purge on a browsed TEMP fixture. Passed 16 Sep 2026 (`tools/run-flaui-e2e.ps1`, 23.6 s, PreferManualDelete, never `C:\Windows.old`).
2. Accessibility pass (keyboard, narrator names, contrast, DPI), dark theme check, window-size limits.
3. Error surfaces: every failure has a user-facing explanation and a log reference; unhandled-exception dialog with redaction.
4. Performance pass on 1M-node fixture; memory ceiling (< 1.5 GB during scan of 1M nodes).
5. README, docs site pages (recipes, limitations, FAQ with SEO terms), THIRD_PARTY_NOTICES, CONTRIBUTING (how to add a rule or recipe), issue templates that ask for the support bundle.
6. Code signing setup: `.github/workflows/release.yml` submits SignPath when `vars.SIGNPATH_ENABLED` is `true` (secret `SIGNPATH_API_TOKEN` plus org/project/policy slugs). ARM64 publish and SHA-256 checksums already ship. Credentials are still absent, so current assets stay unsigned.
7. Manual test matrix on VMs: Windows 10 22H2, Windows 11 24H2/25H2, upgrade-created and reinstall-created Windows.old, non-English Windows (robocopy adapter off, so only UI strings matter), profile with OneDrive Files On-Demand placeholders, EFS files, BitLocker secondary volume.

**Acceptance.** v0.1.0 tag; release assets; README ranks-ready; all invariants tested; manual matrix passed with issues triaged.

---

## Order and dependencies

M0 → M1 → M2 → M3 → (M4 ∥ M5 after M3 and the `IRecipe` infrastructure at the start of M4) → M6 → M7. M4's infrastructure step must land before M5 starts. Total ≈ 12 weeks of effort for v0.1.

## Trimmed from v0.1 during planning

- Robocopy as primary backend (kept as an off-by-default adapter behind `IRestoreCopier`, implemented only if a bulk-speed problem is measured).
- Chromium new-profile transplant beyond bookmarks (feature-flagged).
- Move/rename restore mode, 10-day task extension, WSL 1 migration, Docker Desktop registration, other browsers, registry-based settings, symlink recreation, network sources, localisation, CLI automation.

## Risks and mitigations tracked per milestone

| Risk | Milestone | Mitigation |
|---|---|---|
| Backup-privilege enumeration does not list some deny-ACL folders | M0 | P/Invoke walker fallback (`FileIdBothDirectoryInfo`) |
| Defender flags the single-file EXE | M0/M7 | Minimise native extraction; LiteDB fallback; signing; submit false positive |
| WPF tree performance with 1M nodes | M1 | Flat virtualized list backed by SQL paging; measured early |
| `cleanmgr` handler behaves differently across builds | M0/M6 | Manual deleter is fully featured and tested; handler is preferred but not required |
| Browser formats change (Firefox `logins.db`, profile groups; Chrome profile version) | M4 | Allow-list is data; exports are the guaranteed path; transplant is best-effort with clear messaging |
| Disk space on the same volume | M3 | Budget UI; refuse to start plans that do not fit; documented "move mode" decision for v0.2 |
| 10-day auto-delete during a multi-day recovery | M1/M6 | Prominent countdown; docs explain the risk; v0.2 opt-in extension |
